@model IEnumerable<BookMyShow.Models.LanguageModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Language)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MovieModel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Language)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MovieModel.MovieDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LangId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LangId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LangId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
